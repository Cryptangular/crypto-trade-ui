import { CryptoPairPipe } from './crypto-pair-pipe';

describe('CryptoPairPipe', () => {
  it('create an instance', () => {
    const pipe = new CryptoPairPipe();
    expect(pipe).toBeTruthy();
  });
});
