import { StxPriceChange } from './stx-price-change';

describe('StxPriceChange', () => {
  it('should create an instance', () => {
    const directive = new StxPriceChange();
    expect(directive).toBeTruthy();
  });
});
